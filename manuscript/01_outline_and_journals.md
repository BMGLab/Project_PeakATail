# PeakATail Manuscript Skeleton — Q1 Methods + Application Paper

**Working draft v0.4 (2026-08-21) — numbers moved to the verified v2 run 2026-08-21 from `19_final_gate_v2.md` / `16_trusted_novel_kinnex.md` §v2 / `14_switch_calibration_v2.md` (verified); re-positioned per `13_reliability_positioning.md` (PI decision: reliability first).** Grounded in PeakATail `develop` after PRs #93 (Stage 1b+1c), #96 (IP-filter strand fix) and #97 (performance) were merged by Amir on 2026-08-21 (code `9dfdefb`); the v2 re-run of all four arms executed from the frozen worktree `tools/pa-polya-run-9dfdefb3` (2026-08-21 16:14–16:50) and was verified **FIXED** in `19_final_gate_v2.md`, which supersedes `15_final_gate.md` (the v1 record) for every figure and text number (repo `tools/PeakATail`, docs bmglab.github.io/PeakATail). The clustering-novelty claim is **dropped**: the ablation showed PAS-count clustering is 3'-end expression signal re-encoded (see Supplementary S4). The paper now claims three things — **trustworthy PAS detection (low false positives), reliable cell-type-specific APA switches, and 3'UTR length change** — each tied to a gate pre-registered in `13_reliability_positioning.md` before the final Stage-2/3 run existed; the fourth pre-registered claim, **trusted de novo PAS**, missed its gate (49% vs the 70% long-read target on the v2 output, `16_trusted_novel_kinnex.md` §v2) and is reported as a **negative result**, not a claim. **Stage 1d: MERGED.** The IP-filter minus-strand window bug (#96) and the compute blow-up (#97) were merged on 2026-08-21, the four arms were re-run and re-scored, and every prediction was hit to ≤ 0.001: PBMC atlas-agreement P@100 0.7167 → **0.7062**, mice 0.7414 → **0.7450** and 0.7554 → **0.7572**, gates unchanged (`19_final_gate_v2.md`, verified FIXED).

**Number policy for this file.** Only adversarially verified numbers appear as results (sources: `07_curated_benchmark_report.md`, `09_headtohead_results.md`, `05_figure_index.md` §1–8, `12_stage2_gate.md` Stage-1b verdict, and — for every final-run number — `19_final_gate_v2.md` (the verified v2 run; `15_final_gate.md` is the superseded v1 record) plus the v2 arm score TSVs and the verifier table it cites: `results/benchmark_tools/pbmc_10k_v3/peakatail_clipseeded_final_v2{,_ipfilt}/score_*.tsv`, `results/benchmark_tools/gse104556/peakatail_clipseeded_final_v2/mouse{1,2}/score_*.tsv`, `results/benchmark_tools/final_v2_verify/VERIFIED_v2.md`). Stage-2 / final-run placeholders were filled from `15_final_gate.md` on 2026-08-21 and moved the same day to the v2 numbers of `19_final_gate_v2.md`; the trusted-novel Kinnex check from `16_trusted_novel_kinnex.md` §v2 (verified FIXED on the v2 output; a negative result) and the switch-test calibration from `14_switch_calibration_v2.md` (verified SOUND). Anything that depends on Stage 3 — switch replication on Laughney, spermatogenesis on the final caller, Laughney per-cell-type switches, the quarantined spermatogenesis figure (`11_verifier_corrections.md`) — is still written as **[final run pending]**. "Precision" in this file always means **atlas-agreement precision at 100 bp** against curated PolyASite 2.0 point sites (atlas-novel true sites count as false positives; `15_final_gate.md` §7); recall is shown as detected-gene recall (R_det) with full-atlas recall alongside. The retired dump-based precision figure and the stale Stage-2 gate numbers are never quoted as results; the shipped-caller head-to-head numbers (atlas-agreement precision 0.118 / F1 0.134 PBMC) are quoted only as the *baseline* the fix was measured against. The non-IP PBMC ≥2-molecule file (`pas_tier1_ge2mol_noIP_POSTHOC.bed`) is **not** the pre-registered default and is never quoted as such.

---

## (a) Ranked candidate journals

The ranking is unchanged; the fit argument is re-justified for a reliability/benchmark-centred methods paper whose headline is *how much of a single-cell PAS call set can be trusted, and how to make it trustworthy* — not a biological discovery and not a clustering novelty.

| Rank | Journal | Fit rationale (reliability/benchmark framing) | Format constraints (methods+application paper) |
|---|---|---|---|
| 1 | **Genome Biology** (IF ~12) | Still the best fit, and the fit has *improved*: the Method article type explicitly rewards rigorous benchmarking with nulls and honest comparison, and GB published APAeval — the community's own APA benchmark — so a paper whose core is "pre-registered gates, gene-body-shuffled nulls, long-read truth, replicate reproducibility, and a disclosed negative result that motivated a caller redesign" speaks their language. Sierra and scDaPars precedents remain. Reviewers there will accept a tool that is not the F1 leader if its precision-first tier, its calibration story and its provenance are demonstrably sound. | Method article: no hard word limit, ~6–8 main figures; structured abstract (Background/Results/Conclusions, ≤350 words — our ≤200 w draft fits); open access APC (~$4k); code in a DOI-archived repository (Zenodo) under an OSI license (MIT ✓); mandatory "Availability of data and materials". |
| 2 | **Nature Communications** (IF ~15) | Fit has *weakened*: without a headline biological discovery the paper is a methods/benchmark paper, which NatComms takes only when the benchmark itself changes field practice. Our strongest NatComms argument is now the field-level finding (evidence type, not peak-calling sophistication, discriminates callers; atlas-only benchmarking flatters internal-priming artefacts) plus the Laughney per-cell-type switch application **[pending Amir's rerun]**. Attempt only if Fig 6 yields a replicated, orthogonally supported program. | Article: ~5,000 words (excl. Methods), ≤10 display items, unstructured abstract ≤150 words (trim the draft), Methods unlimited; Reporting Summary + Code Availability mandatory; APC ~$6.8k. |
| 3 | **Genome Research** (IF ~6–7) | Methods/Resource track; excellent fit for accuracy-centred work with nulls and long-read truth (PolyA-seq/Derti lineage is a GR paper). Natural second submission after GB with no restructuring. | ~55,000 characters incl. references; abstract ≤250 words; typically ≤8 figures; software free to academics, data public before review. |
| 4 | **Nucleic Acids Research** (IF ~14–16) | PolyASite and PolyA_DB are NAR papers — a benchmark built on curated PolyASite 2.0 point sites, with the circularity caveats handled explicitly and a pre-registered de novo PAS definition tested *independently* of the atlas against long reads (reported as a negative result), will be read by exactly the right reviewers. Risk unchanged: NAR methods papers often want a resource/webserver flavour. | No rigid length limit (~9 typeset pages); abstract ≤200 words + graphical abstract; open access mandatory (APC ~$5k). Fallback: **NAR Genomics & Bioinformatics**. |
| 5 | **Briefings in Bioinformatics** (IF ~7–9) | Fit has *improved*: BiB's centre of gravity is comprehensive tool comparison, and Fig 3 (six tools × two datasets × two truth sets, one scorer, three-seed nulls, reproducibility metric) is now one of the paper's two strongest assets. The final-run gate was cleared with a large margin (atlas-agreement P@100 0.706–0.757 vs 0.50) but on three call sets from two datasets and at below-polyApipe recall; if reviewers weigh that recall cost heavily, this is the safest high-visibility home. | ~6,000–7,000 words; abstract ≤250 words; "Key Points" box (3–5 bullets); figures ~6–8; code availability mandatory. |
| 6 | **Bioinformatics** (OUP) (IF ~5–6) | Reliable venue (SCAPE, scDAPA). Length limit forces the application and half the benchmark into supplements; viable if we descope to "precision-first caller + calibration". | Original Paper ~7 pages (~5,000 words all-inclusive); structured abstract (Motivation/Results/Availability); reviewers install software. |
| 7 | **PLoS Computational Biology** (IF ~4) | Methods section; no length pressure; its open-science ethos fits the pre-registration, full disclosure of the failed gate and the FDR-failure-and-fix story. Safety net. | No length limit; abstract ≤300 words + lay author summary; unlimited figures; archived open-source software; APC ~$3k. |

**Recommended strategy:** write to Genome Biology's Method format (structured abstract, 6 main figures, pre-registered gates cited in Results). It degrades to Genome Research and BiB without restructuring. Escalate to Nature Communications only if Fig 6 delivers a replicated per-cell-type APA program with orthogonal support.

---

## (b) Title candidates

1. **PeakATail: trusted poly(A)-site detection and reliable cell-type APA switches from standard 3' scRNA-seq** *(safe, descriptive; leads with the two primary claims)*
2. **Precision-first polyadenylation-site calling in single cells: poly(A)-tail read evidence, calibrated testing and replication filters in PeakATail** *(mechanistic; names the three components that buy reliability)*
3. **How many single-cell poly(A) sites can be trusted? A null-referenced, long-read-validated benchmark and a precision-first caller** *(question title; benchmark-centred — best for BiB/GB if the gates clear narrowly)*
4. **Low-false-positive poly(A)-site discovery and replicated 3'UTR switches in single cells with PeakATail** *(compact; fits NatComms/NAR title length)*
5. **Reliable alternative polyadenylation analysis from 3' scRNA-seq: PeakATail's clip-seeded calling benchmarked against curated atlases and long-read truth** *(explicit about the truth sets; longest)*

Retired: every earlier candidate that led with "clustering", "cell-state discovery from PAS usage alone", "expression-independent", or "beyond gene counts".

---

## (c) Abstract draft (≤200 words; bracketed = Stage-3 run pending)

> Alternative polyadenylation (APA) remodels 3'UTRs across cell types, and the poly(A)-site (PAS) signal is present in every 3' scRNA-seq dataset — but single-cell PAS calls are rarely benchmarked against independent truth, and on deep 10x data coverage-shape callers, including our own earlier version, emit call sets in which most sites lack atlas support. We present PeakATail, an aligner-agnostic framework that seeds PAS from non-templated poly(A) soft-clipped reads, reports clip-supported and coverage-only sites as explicit tiers with per-site molecule counts, filters internal priming, and tests cell-type APA switches with calibrated, replication-filtered statistics. Against curated PolyASite 2.0 point sites with gene-body-shuffled nulls (0.013–0.022), PeakATail's pre-registered default reaches atlas-agreement precision (100 bp) of 0.706 on PBMC and 0.745/0.757 on two testis replicates (gate ≥ 0.50) at detected-gene recall 0.18–0.21, below polyApipe's, and 76% of its PBMC sites lie within 25 bp of a PacBio Kinnex poly(A)-verified long-read 3' end (≥5 UMI); a six-tool benchmark shows that evidence type, not peak-calling sophistication, separates callers. A pre-registered "trusted novel PAS" definition missed its 70% long-read target (49%), reported as a negative result. PeakATail recovers the spermatocyte→round→elongating spermatid 3'UTR shortening gradient [ρ] and maps replicated per-cell-type switches across [17] lung-cancer samples. Open source (MIT): github.com/BMGLab/PeakATail.

*(199 words by `wc -w`; the v2 renumbering did not change the length. Trim to ≤150 for NatComms by cutting the benchmark clause. Benchmark numbers are from the v2 run (`19_final_gate_v2.md`, verified FIXED); the Kinnex concordance (0.7647 of default PBMC sites at ≥5 UMI, 25 bp, x3p truth) and the trusted-novel negative result (0.4853 vs 0.70) from `16_trusted_novel_kinnex.md` §v2 (verified); "calibrated" = Fisher cells-mode with marker pre-selection off, 3.0% null p < 0.05 (`14_switch_calibration_v2.md`, verified). The remaining brackets ([ρ], [17]) are filled from the Stage-3 run only — never from the stale run. Caption/Methods must add: full-atlas recall 0.0889 (PBMC) and 0.0977/0.0995 (mice); single PBMC donor; the two mice are one study and chemistry. The Stage-1d re-run has landed and the PBMC value moved 0.7167 → 0.7062, as predicted.)*

---

## (d) Figure-by-figure plan

### Fig 1 — Tool overview: clip-seeded, tiered PAS calling and the reliability pipeline
- **A.** Pipeline flow: tagged BAM(s) → per-strand read ingestion with UMI dedup and SAM-flag filtering (−F 3844 semantics) → **clip-seeded PAS calling** (poly(A) soft-clip clusters seed candidates; coverage summits without clip support are emitted as a second tier) → internal-priming filter (genome FASTA) → PAS×cell matrix → tiered gene-end/3'UTR annotation → `switch diff / length / trend / geneview`. (Base: `pipeline_flow_report.png`, to be redrawn.)
- **B.** Evidence schematic: why coverage shape alone is insufficient on deep data (clip reads sit ~90–105 nt past the coverage peak end — the cleavage-offset finding, verified in `07_curated_benchmark_report.md` §3) and how clip seeding recovers evidence that falls outside every coverage peak window.
- **C.** The two output tiers and the default: tier-1 = clip-supported with ≥2 distinct molecules and IP-pass (default output); tier-2 = coverage-only, written to a separate `low_confidence` file; the ≥1-molecule output as the sensitivity arm. State here that the default was pre-registered in `13_reliability_positioning.md` before the final run (commit `0e27b1a`, 2026-08-21 01:19; arms started 02:59 — `15_final_gate.md`).
- **D.** Worked gene example: one 3'UTR, proximal/distal PAS, two cell types, with clip-support counts per site (real `switch geneview` track, regenerated from the final run).

### Fig 2 — Trustworthy PAS detection: atlas-agreement precision vs curated atlas and long-read truth, with nulls, two tiers
**Figure file: `figures/final_benchmark` (`manuscript/figures/final_benchmark.{png,pdf}`, PNG 300 dpi + PDF fonttype 42; every plotted value to `results/figures/manuscript/final_benchmark*.tsv`; caveats printed on the figure; colourblind palette per `figures/README.md`). Source of every number: `19_final_gate_v2.md` (the verified v2 run, FIXED) and the v2 arm score TSVs it cites; `15_final_gate.md` is the superseded v1 record. Built panels: **a** PBMC atlas-agreement P@100 vs R_det@100 with PeakATail as a connected path of operating points (shipped → both tiers no IP → tier-1 ≥1 mol no IP → tier-1 ≥1 mol IP → precision default), F1_det isolines, both gate lines, null ticks; **b** GSE104556 testis, two mice as mean ± range; **c** n sites per call set (log); **d** PBMC IP-arm tier decomposition (tier-2 0.0558 / tier-1 ≥1 mol 0.3520 / default 0.7062; 72.1% of tier-1 sites single-molecule). Outline items A–B below are panels a–c, F is the T3 inset; C–E are supplementary/long-read panels drawn from `16` and the IP analysis, not part of `final_benchmark`. Caption text: `figures/final_benchmark.caption.md`. The figure is being regenerated on the v2 run (PBMC default 0.7167 → 0.7062); the file reference `figures/final_benchmark` is unchanged.**
- **A.** Atlas-agreement precision (100 bp) vs curated PolyASite 2.0 representative sites (strand-matched, point mode) for the default output, the ≥1-molecule tier-1 output and tier-2, on pbmc_10k_v3 and both GSE104556 testis mice, with the 3-seed gene-body-shuffled null drawn on every bar. **v2 run (verified FIXED, `19` §1):** default = clip-seeded tier-1 ∩ IP-pass ∩ ≥2 distinct molecules — **PBMC 0.7062** (n 46,524; null 0.0217/0.0217/0.0217), **mouse 1 0.7450** (n 26,255; null 0.0149/0.0129/0.0139), **mouse 2 0.7572** (n 26,526; null 0.0137/0.0127/0.0150) — gate **P ≥ 0.50 PASS on all three**, null 33–55× below. ≥1-molecule tier-1 (sensitivity arm): PBMC 0.3520 IP (n 167,565) / 0.3032 no-IP (n 222,955, byte-identical to v1), mice 0.5686 / 0.5880; tier-2: PBMC 0.0568 no-IP / 0.0558 IP, mice 0.1191 / 0.1142; both tiers: PBMC 0.1932 no-IP / 0.2044 IP, mice 0.4488 / 0.4539. Gate lines: **P ≥ 0.50** (default, pre-registered) and **P ≥ 0.38 / F1_det > 0.261** (original two-sided gate, reported for the ≥1-molecule output: F1_det 0.3001–0.3046 passes, **no ≥1-molecule output clears P ≥ 0.38**). The non-IP PBMC ≥2-molecule file (`pas_tier1_ge2mol_noIP_POSTHOC.bed`, n 62,110, P 0.591) is **not** the pre-registered default (no IP filter) and, if shown at all, is a separately labelled post-hoc point.
- **B.** Precision–recall curves across windows (10/25/50/100/200 bp) with recall on the detected-gene denominator (285,136 PBMC / 126,686 testis sites). **v2 run (verified, `19` §1):** default output **R_det@100 0.1754 (PBMC) / 0.2048 / 0.2080 (mice)**, F1_det 0.2811 / 0.3213 / 0.3264; PBMC default across windows P 0.521 / 0.639 / 0.674 / 0.706 / 0.737 and R_det 0.084 / 0.105 / 0.135 / 0.175 / 0.220 (v2 score TSV). ≥1-molecule tier-1 R_det: PBMC 0.2685 IP / 0.2970 no-IP, mice 0.2806 / 0.2826. **Caption must state full-atlas recall next to R_det: 0.0889 (PBMC; 569,005 sites) and 0.0977 / 0.0995 (mice).** Recall ceilings stated (only ~29% of detected-gene atlas sites carry any clip read in this BAM); the scored set is the post-cell-filter set (`pasbed.bed` drops zero-count gene-assigned PAS: 0.45% PBMC, 3.7% mouse 1).
- **C.** Atlas-independent truth: Kinnex PBMC long-read concordance (≤25 bp, strand-matched, x3p truth; support tiers ≥5/≥20/≥100/≥500 **UMI** — support is counted in UMIs, not records). **Landed, v2 (`16_trusted_novel_kinnex.md` §v2, verified FIXED; every count reproduced by an adversarial verifier):** the full PBMC precision default (n 46,524) has a poly(A)-verified Kinnex 3' end within 25 bp for **0.7647 / 0.5584 / 0.2807 / 0.0976** of sites at ≥5 / ≥20 / ≥100 / ≥500 UMI; the atlas-known, hexamer-pass complement (n 28,453) 0.8941 / 0.7072 / 0.3885 / 0.1415; the atlas-novel "trusted-novel" subset (n 7,259) 0.4853 / 0.2393 / 0.0657 / 0.0118; gene-body-shuffled null ≈ 0.0074 at ≥5 UMI (10 seeds). Robustness across truths: trusted-novel 0.5154 under the GEM-X truth and 0.5816 pooled (0.6688 at 50 bp, 0.7143 at 100 bp), while the atlas-known complement again scores ~0.89–0.90 — donor mismatch does not explain the atlas-novel gap. Internal priming: 27.0% of trusted-novel sites lie within 25 bp of a Kinnex internal-priming decoy terminus vs 7.6% of the atlas-known reference (13.0% for the full default). Kinnex termini sit a few bp upstream of PeakATail cleavage points (v1 counts, not recomputed for v2: 1,228 hits at −5..0 vs 210 at 0..5; no effect at 25 bp). A genuine / IP / unsupported decomposition of the *full* default per support tier is not in `16` and is not drawn. Not part of `figures/final_benchmark`; `figures/kinnex_truth_validation` stays quarantined — redraw from `results/reliability/trusted_novel_final_v2_pbmc/`.
- **D.** Sequence architecture (verified): AATAAA|ATTAAA within +0..+100 nt of the coverage-peak end 37.1% vs 14.7% null; modal hexamer +75 nt; A-fraction crest 0.426 at +98 nt — and the same panel re-anchored on clip-seeded cleavage sites, where the canonical −40..−5 window should now carry the signal — **landed in part (`16` §v2, verified): 76.7% of PBMC precision-default sites (35,712 / 46,524) carry one of the 12 canonical hexamers in −40..−5 of the clip-seeded cleavage point; among atlas-novel sites the hexamer adds nothing to long-read concordance (hexamer-fail 0.4874 vs hexamer-pass 0.4853 at ≥5 UMI), +0.046 on the whole set (0.7647 → 0.8110). A shuffled-position null for the re-anchored window is not in `16` and must be computed before the panel is drawn.**
- **E.** Internal-priming filter effect: A-rich downstream tracts 6.3% → 3.0% (null 2.2%) on the shipped arm (verified). **v2 run (verified, `19` §1):** the IP filter alone buys **+5.3 pp atlas-agreement precision for −1.2 pp R_det** (mouse 1, ≥2-molecule; the v1 wording "+4.9 / −1.4" is superseded); on PBMC tier-1 ≥1-molecule n goes 222,955 (no IP) → 167,565 (IP) with P 0.3032 → 0.3520. **Stage 1d, MERGED (#96):** the v1 run applied the forward-coordinate IP window to both strands (`15_final_gate.md` §6); the corrected strand-aware rule was merged on 2026-08-21 and the four arms were re-run. Verified impact on PBMC (`19` §1): '+' strand 0 changes, '−' strand 5,599 sites newly removed and 11,217 newly kept, residual 0 in the verifier's key-for-key reconstruction — default n 44,394 → 46,524 and P 0.7167 → **0.7062** (−1.1 pp P, +0.5 pp R_det); mice 0.7414 → **0.7450** and 0.7554 → **0.7572** (the full v2 sets also restore 560 / 589 sites the old rule wrongly dropped); **the P ≥ 0.50 gate is unchanged**. An independent re-implementation of the corrected rule flags 0 surviving sites on either strand in all three arms, and the v2 trusted-novel analysis re-derived IP from the genome (36–661 sites, ≤3% change, `16` §v2). Any per-site IP flag from a pre-fix (v1) run must be recomputed (`ema reannotate --genome-fasta`), never trusted.
- **F.** Gate table inset (= T3): each pre-registered gate, the value, pass/fail — no arm omitted. **v2 run (`19`; the gates themselves are unchanged from their pre-registrations and v2 was re-run against them):** default P@100 ≥ 0.50 — PBMC 0.7062 PASS, mouse 1 0.7450 PASS, mouse 2 0.7572 PASS; original two-sided gate on the ≥1-molecule PBMC output — F1_det 0.3046 (IP) / 0.3001 (no-IP) > 0.261 pass, P 0.3520 / 0.3032 < 0.38 **FAIL**; both-tiers PBMC 0.1932 (no-IP) / 0.2044 (IP) FAIL on both sides. Disclose on the figure: gate and default pre-registered at 01:19, arms started 02:59 (2026-08-21); the ≥2-molecule threshold was chosen after a stale Stage-2 sweep (S9).

### Fig 3 — Head-to-head trade surface and replicate reproducibility
- **A.** Six tools (PeakATail default + sensitivity arm, polyApipe, SCAPTURE, Sierra, scAPAtrap; scUTRquant shown but unranked as annotation-based), two datasets, one scorer (`score_tool.py`), per-dataset detected-gene denominators, 3-seed nulls. Shipped-caller baseline row kept for transparency: PBMC P 0.118 / R 0.156 / F1 0.134 (last among de novo tools); testis F1 0.258/0.260 (second). polyApipe reference PBMC 0.380 / 0.199 / 0.261. **v2 PeakATail rows (verified, `19` §1–2):** PBMC precision default n 46,524 / P 0.7062 / R_det 0.1754 / F1_det 0.2811; tier-1 ≥1-mol IP 167,565 / 0.3520 / 0.2685 / 0.3046; tier-1 ≥1-mol no-IP 222,955 / 0.3032 / 0.2970 / 0.3001 (byte-identical to v1). Competitors (same scorer, same denominators): scUTRquant* 40,519 / 0.793 / 0.178 / 0.290 (catalog, unranked); SCAPTURE 35,759 / 0.652 / 0.118 / 0.199; polyApipe 120,916 / 0.380 / 0.199 / 0.261; Sierra 106,170 / 0.256 / 0.135 / 0.177; scAPAtrap 787,138 / 0.100 / 0.300 / 0.150; shipped PeakATail 277,111 / 0.118 / 0.156 / 0.134. Mouse m1/m2 (P / R_det / F1_det): precision default 0.7450/0.7572 · 0.2048/0.2080 · 0.3213/0.3264; tier-1 ≥1-mol IP 0.5686/0.5880 · 0.2806/0.2826 · 0.3758/0.3818; scUTRquant* 0.782/0.784 · 0.259/0.259 · 0.389/0.389; SCAPTURE 0.694/0.672 · 0.155/0.147 · 0.253/0.241; polyApipe 0.401/0.412 · 0.250/0.251 · 0.308/0.312; Sierra 0.509/0.581 · 0.107/0.117 · 0.177/0.195; scAPAtrap 0.250/0.239 · 0.245/0.259 · 0.248/0.248; shipped 0.369/0.368 · 0.198/0.201 · 0.258/0.260. scTail has no number (R1 = 28 bp; BLOCKED). **One-liner (verifier's wording):** the precision default is the most atlas-concordant de novo call set in the panel on both datasets, at polyApipe-or-lower recall (PBMC −12%, mouse −18% / −17% vs polyApipe) and essentially tied F1 (+0.020 PBMC, +0.013 / +0.014 mouse); the F1 lead is not a meaningful margin — the claim is precision, not overall accuracy.
- **B.** Trade surface: atlas-agreement precision vs R_det with call count as marker size (21k–787k, 37-fold range) — the figure argues that single-number rankings are misleading; per-dataset reporting is mandatory (shipped PeakATail atlas-agreement precision spans 0.118 PBMC → 0.369 testis → 0.447 Laughney).
- **C.** Replicate reproducibility@100 bp on the two testis mice: scAPAtrap 0.79–0.88, Sierra 0.79–0.83, PeakATail (shipped) 0.73–0.75, polyApipe 0.49–0.50; depth stratification as mechanism (62% of polyApipe's mouse calls are depth-1 and reproduce 0.31 vs 0.81 for depth ≥2). Honest scope: PeakATail's reproducibility advantage is **against polyApipe only**; scAPAtrap's lead is measured after its own `reducePeaks` depth cleaning. Final-run PeakATail replicate concordance **[final run pending — not in `15_final_gate.md`; compute on the two mouse `pas_PRESPEC_precision_default.bed` files with the same concordance script]**.
- **D.** Long-read re-ranking: Spearman ρ between atlas and Kinnex precision orderings (0.90 at 7 of 8 arms, 0.80 at one — never say "every"); Sierra's atlas precision was ~half internal priming, a caution for atlas-only benchmarking. **[quarantined; regenerate]**
- **E.** Compute: runtime and peak RSS per tool — **state the footprint plainly, and state that it changed.** v2 (verified, `19` §3; #97 merged 2026-08-21): all four arms ran **concurrently** on one box — PBMC default arm (no IP, `--threads 16`) wall 34:37, peak RSS **12.53 GB**, CPU 785%; PBMC IP arm 32:59 / 11.12 GB / 818%; mouse 1 / mouse 2 (IP, `--threads 12`) 14:10 / 3.07 GB and 16:00 / 3.65 GB. Uncontended single-run PBMC is 27m43s, so quote **peak RSS as the production number and disclose the concurrency whenever wall time is quoted** (~28–35 min on PBMC). That is 23.5× / 21.5× less peak RSS on PBMC and 7.5× / 6.1× on the mice than v1 (3:45:53 / 293.7 GB at ~1.4 cores despite `--threads 16`), so the **≥300 GB node requirement is gone**: a 10k-cell PBMC BAM now needs ~12.5 GB. Panel context: polyApipe 3:26 h, Sierra 2:43 h, scUTRquant 0:30 h. The v1 footprint and the `10_caller_fix_plan.md` stop signal (RSS > 150 GB → profile first) are reported as the **resolved Stage-1d item**, not as a current limitation. Plus the `reannotate` re-branching cost argument.
- **F.** Fairness checks as a small table: all tools ≥95% gene-proximal (verified), so PeakATail's `max_gene_distance` restriction does not bias the comparison; gene filter is F1-neutral on raw output.

### Fig 4 — Reliable cell-type APA switches: calibration and the replication filter
- **A.** The failure (verified, shipped tests on mis-keyed matrices): label-shuffle null p-value histograms — Fisher 38.6% and NB-pairwise 21.3% of null tests p < 0.05; 20/20 null runs report q < 0.05 hits. This is reported as a finding, not hidden. On correctly keyed matrices (v2, `14_switch_calibration_v2.md`, verified SOUND) the shipped defaults are still anti-conservative: reads-mode Fisher 20.3%, cells-mode Fisher with the default top-200 marker pre-selection 13.0%, `nb_pairwise` 24.7% null p < 0.05; 19–20/20 null runs carry q < 0.05 hits.
- **B.** The fix (`14_switch_calibration_v2.md`, verified SOUND; testis mouse 1 Stage-1b acceptance run, 1,230 GEX-labelled cells (SPC 370 / RS 504 / ES 356) × 76,711 PAS, 20 label permutations shared by all arms, 6 arms × 21 runs, 0 failures): **only Fisher `--count-mode cells` with marker pre-selection off (`--marker-top-n 0`) controls FDR — 3.0% null p < 0.05, 0.00% null q < 0.05, 0/20 null runs (0/60 BH families) with any q < 0.05 hit; conservative, not exactly calibrated** (24.8% of null p exactly 1; 2.94–3.13% in every stage pair; 2.6–4.0% in every expression stratum). Mechanism: marker pre-selection is a label double-dip (restricting the calibrated arm's null p-values to the permuted-label marker set gives 17.4% p < 0.05) and also changes the Fisher gene denominator (only 629/6,453 p-values identical between marker-on and marker-off on the same tests); reads-mode Fisher is inflated by UMI pseudoreplication (9.6% null p < 0.05, ~1,500 false q < 0.05 hits per null run even with pre-selection off); `nb_pairwise` is tail-inflated by the 1e-4 dispersion floor (5.1% bulk; the floor carries 67% of its null q < 0.05 hits; min null p 4e-182). **Decision (pre-registered rule, `13_reliability_positioning.md` §2):** default reliable test = Fisher cells-mode, marker pre-selection off, nominal BH q; **permutation-calibrated q (label-shuffle null, ≥20 permutations) is required whenever marker pre-selection or `nb_pairwise` is used** (TRUE hits surviving q_perm < 0.05: 186/307 reads-Fisher, 640/668 cells-Fisher, 1,733/1,754 nb). Figure: `figures/fdr_calibration_v2`. Caveats on the figure: single mouse, single tissue, very large true stage effects (TRUE hit counts are upper bounds on detectability, not precision estimates); null-run hit counts are not comparable to TRUE counts; KS-vs-uniform rejects for every arm (reported, not gated); "calibrated" is our operational rule, not a standard; cross-dataset confirmation on Laughney is **[final run pending — Stage 3]**.
- **C.** Replication filter: a switch is reported only if called in the same direction in ≥2 independent samples (testis 2 mice; Laughney 17 samples); number of switches before/after, and the expected false-positive rate of the filter from between-donor same-cell-type contrasts (the specificity null of `02_validation_plan.md`). **[pending]**
- **D.** Effect-size floor |ΔPDUI| ≥ 0.1 alongside q; volcano with both thresholds; canonical positive controls (IGHM plasma-vs-B, proliferation-linked global shortening) with correct direction **[pending]**.

### Fig 5 — 3'UTR length biology and trusted de novo PAS
- **A–C (= panel a; `figures/spermatogenesis_control` quarantined). Spermatogenesis positive control (GSE104556, both mice).** Per-cell 3'UTR length index across spermatocyte → round spermatid → elongating spermatid: **SPC>RS>ES is the claim** (ρ −0.73 to −0.86 under every labelling tried on the quarantined run); the spermatogonia first step is fragile (inverts under marker-argmax labels, rests on 64/68 cells) and goes to supplement. Per-gene replicate ρ (0.874 on the quarantined run), 6/6 literature genes in the expected direction, 200-permutation null; the intronic/other-exon peaks that flip the sign shown explicitly, and the `wul` vs `wdi` index discrepancy resolved before the atlas arm is used. **[all numbers final run pending; regenerate per `11_verifier_corrections.md`]**
- **D–F (= panel b, `figures/trusted_novel_funnel`, being built). Trusted de novo PAS — NEGATIVE RESULT.** Pre-registered definition (`13_reliability_positioning.md` §3, set before any number existed): clip-supported ∧ ≥2 molecules ∧ not IP-flagged ∧ canonical hexamer within −40..−5 nt ∧ ≥100 bp from any PolyASite 2.0 / PolyA_DB site; target **≥70%** within 25 bp (strand-matched) of a poly(A)-verified Kinnex 3' end, against a shuffled-position null. **Verified on v2 (`16_trusted_novel_kinnex.md` §v2, FIXED; every count re-derived from the primary inputs by an adversarial verifier):** funnel on the PBMC precision default (IP arm, code `9dfdefb`) — 46,524 sites on primary contigs → clip-supported / ≥2 molecules / not-IP 46,524 (the run was already IP-filtered; genome re-derivation flags 36–661, ≤3%) → hexamer (any of 12) **35,712 (76.7%)** → ≥100 bp from PolyASite 2.0 **7,259 trusted-novel (15.6% of input)** → AATAAA/ATTAAA **4,329 strong (9.3%)**; 3,578 genes; PolyA_DB could not be used (hg19 only). Long-read concordance ≤25 bp at ≥5 UMI (x3p truth, Wilson 95% CI): **trusted-novel 0.4853 [0.474–0.497] — target 0.70 NOT MET**; strong-hexamer subset 0.5024; atlas-known hexamer-pass complement 0.8941 (calibration); full precision default 0.7647; atlas-novel hexamer-fail 0.4874 (diagnostic); ≥1-molecule sensitivity set (n 40,077) 0.2894; at ≥20 / ≥100 / ≥500 UMI trusted-novel 0.2393 / 0.0657 / 0.0118. Gene-body-shuffled null ≈ 0.0074 (10 seeds) → enrichment ~65–180×, empirical p at the 1/11 floor — the sites are far from random, but the target is an absolute fraction. Missed under every truth choice (GEM-X 0.5154; x3p+GEM-X pooled 0.5816 at 25 bp, 0.6688 at 50 bp, 0.7143 at 100 bp); donor mismatch cannot explain it (atlas-known ~0.89–0.90 under the same truths). Composition: **69.4% intronic**, only 14.9% in annotated 3'UTRs (atlas-known complement: 69.9% 3'UTR); **27.0% within 25 bp of a Kinnex internal-priming decoy terminus** vs 7.6% for the atlas-known reference — the caller's IP rule (−10..+30, ≥6 A or ≥70% A) is looser than Kinnex's (+1..+18, ≥12 of 18 A), so the residual false-positive class is internal priming the filter does not catch → the `--ip-rule kinnex` option (issue #95 addendum), still open after #96/#97: the corrected filter flags fewer raw peaks (110,000 vs 119,518), so the residual is the leniency of the rule, not the strand bug. The atlas-novelty stage costs the most concordance (0.8110 → 0.4853); the hexamer stage adds nothing among atlas-novel sites. Exploratory stratification (chosen after seeing the data — NOT a re-definition): 3'UTR trusted-novel sites 0.769 at ≥5 UMI / 0.550 at ≥20; ≥5-molecule sites 0.692–0.728 / 0.491–0.625; 2-molecule sites 0.401 / 0.144. **What the paper says:** atlas-novel calls are strongly enriched for real cleavage sites, but the pre-registered "trusted de novo PAS" definition did not reach its 70% target (49% on v2; 52% on v1) — reported as a negative result with the funnel figure. Re-running the unchanged definition on the Stage-1d-corrected caller moved it **down**, not up, so the negative result is not an artefact of the IP bugs (`16` §v2, verified FIXED). A stricter v2 definition (3'UTR-restricted and/or ≥5 molecules and/or a Kinnex-style IP rule) may be pre-registered and must be validated on data not used here (a held-out truth set) before any de novo site is called "trusted"; until then de novo sites ship with molecule support, hexamer tier, feature class and IP status, and the paper makes **no "trusted novel" claim**. The geneview vignette of a "long-read-confirmed novel site" is dropped. Files: `results/reliability/trusted_novel_final_v2_pbmc/` (REPORT_PROVISIONAL.md, status VERIFIED; v1 in `results/reliability/trusted_novel_final_pbmc/`), tool `scripts/reliability/trusted_novel_pas.py` (same file/version for both runs).

### Fig 6 — Application: replicated per-cell-type APA switches in lung cancer (Laughney GSE123904, 17 samples)
- **A.** Cell-type annotation (GEX-derived labels; PeakATail does not claim to define cell types) and per-cell-type PAS coverage.
- **B.** Per-cell-type switches across Normal → Stage I → Metastasis with the replication filter (≥2 samples, same direction) and calibrated q; counts per cell type.
- **C.** TAM/myeloid 3'UTR programs as the worked case; direction calls from `switch length`; pathway enrichment of replicated switches.
- **D.** Geneview vignettes for 2–3 headline genes; orthogonal support (bulk 3'-seq or atlas) for at least one.
- **Status: entirely pending Amir's #67 SWITCH_CELLTYPE rerun on re-keyed, counts-layer matrices with the fixed `main.nf` and `--count-mode cells`.** If the rerun slips, Fig 6 folds into Fig 4 as the replication-filter demonstration and the paper ships as methods + benchmark.

### Supplementary figures
- S1. Dataset summaries (cells, depth, chemistry, QC; clip rate per BAM — CellRanger vs STARsolo, since any pipeline that trims poly(A) before alignment destroys the evidence).
- S2. Peak-call QC: width, per-gene PAS counts, tier composition, molecule-count distributions; cleavage-offset analysis on the shipped caller (verified) vs clip-seeded cleavage placement.
- S3. Strategy comparison in-framework (`original`, `lambda_poisson`, `sierra_iterative`, `lambda_gradient`, `clip_seeded`) on identical regions — the verified result that the old dump-based atlas could not rank them (spread 0.004) and the curated-atlas spread (0.355–0.492) that can.
- S4. **PAS-profile clustering as an optional utility (the dropped claim, stated honestly).** TF-IDF/LSI/Leiden on PAS counts recovers GEX cell types (PBMC AMI 0.708 / ARI 0.502; Laughney median AMI 0.662 / ARI 0.463, 17/17 samples, verified). **Ablation: collapsing the 275,370 sites to 14,891 gene totals gives AMI 0.698 — the site resolution adds nothing, so this is 3'-end expression signal re-encoded, not isoform choice.** The within-gene usage-fraction spaces (the isoform-only test) were computed on mis-keyed matrices and are re-run on re-keyed ones **[pending]**; the claim does not depend on their outcome. Offered as a convenience when no GEX labels exist, nothing more.
- S5. Statistical validation extended: `count_mode="cells"` vs `"reads"`; `nb_multi` `sample_split`; min-cells sensitivity; permutation-calibration runtime.
- S6. **The FDR-failure-and-fix story as a methodological contribution.** (i) Null-calibration design (label-shuffle, 20 perms, resumable); (ii) the three causes found — read-level pseudoreplication, marker double-dip, NB dispersion floor at 1e-4 carrying ~half the false hits; (iii) the independent matrix mis-keying bug (99.87% of annotated PAS carried another PAS's counts on testis mouse1; bisected to 2026-03-23) that voided every count-based result and the `test_matrix_pas_id_row_alignment` regression test; (iv) the strand inversion in `switch length` (100% of minus-strand genes) and its CI guard; (v) calibration after the fix (`14_switch_calibration_v2.md`, verified SOUND): only Fisher cells-mode with marker pre-selection off controls FDR (3.0% null p < 0.05, 0/20 null runs with any q < 0.05 hit; conservative), the shipped defaults stay anti-conservative (20.3% / 13.0% / 24.7%), marker pre-selection also changes the Fisher denominator, and permutation-calibrated q is mandatory whenever pre-selection or `nb_pairwise` is used. Framed as "what it takes to make single-cell APA statistics trustworthy", with a checklist other tools can apply.
- S7. Internal-priming characterization: genomic A-tract definitions, flag rate per dataset (3.8% on test data, verified live and off by default in v0.2.0; v2 effect: the IP filter buys +5.3 pp P for −1.2 pp R_det on mouse 1 (`19` §1); the minus-strand window defect of `15_final_gate.md` §6 — found by the verifier, fixed in #96, merged 2026-08-21, four arms re-run: PBMC default n 44,394 → 46,524 and P 0.7167 → 0.7062 (5,599 minus-strand sites newly removed, 11,217 newly kept, '+' strand unchanged), mice 0.7414 → 0.7450 and 0.7554 → 0.7572, gates unchanged; the caller's rule (−10..+30, ≥6 A or ≥70% A) vs Kinnex's (+1..+18, ≥12/18 A) and the resulting 27.0% vs 7.6% Kinnex-IP-decoy rate of atlas-novel vs atlas-known sites on the v2 output — the corrected filter flags fewer raw peaks (110,000 vs 119,518), so the residual is the leniency of the rule, not the strand bug (`16` §v2)), long-read-measured IP fraction per tool **[regenerate; Stage 3]**.
- S8. Benchmark reproducibility: environment archaeology and bug fixes every compared tool needed; `score_tool.py` contract; null construction; truth-set size dependence of F1 (why F1 is quoted only with a named truth set).
- S9. Gate history: the original two-sided gate, the stale Stage-2 run's failure, the post-hoc threshold sweep examined and disclosed (numbers not reported as results; `12_stage2_gate.md` CORRECTION: the sweep was post hoc and counted raw clip reads, not molecules), the pre-registered default that followed, and the final-run outcome. **Full disclosure (verified from git, `15_final_gate.md`):** the precision-first default and its P ≥ 0.50 gate were committed in `0e27b1a` at 2026-08-21 **01:18:59**, before the final arms started at **02:59:36**; the ≥2-molecule threshold was informed by the stale Stage-2 sweep and pre-registered before the final run — both facts are stated. Final-run outcome (v1): default atlas-agreement P@100 0.717 / 0.741 / 0.755, gate PASS on all three call sets. **v2 (`19_final_gate_v2.md`): the Stage-1d re-run was scored against the same, unchanged pre-registered gates — no gate, window, denominator, null design or default definition was altered — and the default gives P@100 0.7062 / 0.7450 / 0.7572, PASS on all three call sets**; the original two-sided gate still fails on atlas-agreement precision for every ≥1-molecule output (P@100 0.3032–0.3520 < 0.38).
- S10. Compute scaling detail; `collapse`/`combine` multi-library workflows; `reannotate` branching.
- S11. Spermatogonia step and the `wul`/`wdi` index reconciliation (from Fig 5 corrections).

### Supplementary tables
- T1. Full 19-tool feature comparison (from `combined_polya_scrna_methods.csv`), with the evidence-type column (coverage / poly(A) clip / sequence model / catalog) added.
- T2. Per-tool, per-dataset, per-window atlas-agreement precision / R_det / full-atlas recall / F1_det with null values (`benchmark_consolidated.tsv`; final-run rows from `results/figures/manuscript/final_benchmark*.tsv`).
- T3. All pre-registered gates with values and pass/fail.
- T4. Replicated differential APA results per contrast (findings_long export), with per-sample direction calls.
- T5. Atlas-novel PAS list from the pre-registered funnel (7,259 sites on the v2 output; 6,628 on v1) with molecule support, hexamer tier, feature class, IP status and Kinnex outcome — no "trusted" label (`16` §v2).
- T6. PDUI/length scores per gene × cell type (length_long export).
- T7. Software versions, parameters, frozen commit hashes, `run_config.json` / `run_manifest.json` digests for every run (runs must freeze their code tree — a lesson from Stage 2).

---

## (e) Section-by-section outline

### Introduction (~5 paragraphs)
1. APA biology: 3'UTR isoform choice controls stability/localization/translation; remodeled in proliferation, cancer, immune activation.
2. 3' scRNA-seq reads pile near poly(A) sites — the signal is in every 10x dataset, but deep data also produces abundant coverage peaks that are *not* PAS, and single-cell PAS call sets are rarely validated against independent truth.
3. Existing single-cell APA tools (Sierra, scAPAtrap, SCAPTURE, polyApipe, scUTRquant, SCAPE, scDaPars, scTail, …) differ mainly in evidence type (coverage shape / poly(A) soft-clips / sequence models / catalogs); published benchmarks rarely use nulls, per-dataset denominators or long-read truth; differential tests are typically not calibrated and not replicated.
4. Gap → PeakATail: clip-seeded, tiered, IP-filtered PAS calling with per-site molecule support; calibrated and replication-filtered switch testing; a pre-registered, atlas-independent long-read test of de novo PAS (reported as a negative result); all within one aligner-agnostic, provenance-tracked framework with pre-registered gates.
5. Contributions summary: (i) precision-first PAS detection validated on two truth sets with nulls; (ii) a six-tool benchmark that reframes the field's accuracy claims; (iii) calibrated, replicated cell-type switches; (iv) UTR-length biology positive control; (v) a pre-registered long-read test of de novo PAS that missed its target, reported as a negative result; (vi) open source with full disclosure of the negative result that drove the redesign.

### Results

**R1. PeakATail: clip-seeded, tiered PAS calling in an end-to-end reliability pipeline** *(descriptive; Fig 1)*
- **Key claim:** PeakATail turns any CB/UB-tagged BAM into tiered, evidence-annotated PAS calls and calibrated single-cell APA tests through one modular pipeline.
- **Exact evidence:** architecture description; the clip-seeding algorithm (25-bp single-linkage clusters of poly(A) soft-clip sites, read-weighted mode as cleavage position, UMI-deduplicated support, coverage summits without clips as tier-2); IP filter; UMI dedup / flag filtering; `reannotate` branching; provenance (ledgers, content-hashed manifests, frozen commit). Verified engineering evidence: on the chr19+21 slice the clip-seeded caller lifted F1@100 from 0.177 (shipped) to 0.291 both tiers / 0.348 tier-1, independently re-scored (Stage 1, verified SOUND); the full v2 run (frozen worktree `tools/pa-polya-run-9dfdefb3`, code `9dfdefb`, flags per arm confirmed from `run_config.json`, `LC_ALL=C`, BEDs `sort -c` clean, suite 1,277 passed) is the provenance example, and its per-site molecule counts show why the tier/molecule labels matter: 72.1% of PBMC tier-1 sites are single-molecule (mouse ≈ 50%) and are discarded by the default (`19` §4, the verifier recount of `15_final_gate.md` §4). No gate applies here; the full-run accuracy numbers are quoted in R2 only.

**R2. PeakATail's default output is a low-false-positive PAS set against curated-atlas and long-read truth** *(Fig 2)*
- **Key claim:** the pre-registered default (clip-supported, ≥2 distinct molecules, IP-pass) achieves **atlas-agreement precision (100 bp) of 0.7062 on pbmc_10k_v3 and 0.7450 / 0.7572 on the two testis mice** against curated PolyASite 2.0 point sites — gate P ≥ 0.50 **PASS on all three call sets** (`13_reliability_positioning.md` §1; `19_final_gate_v2.md` §1, verified FIXED), 33–55× the gene-body-shuffled null (3 seeds: PBMC 0.0217/0.0217/0.0217; mice 0.0149/0.0129/0.0139 and 0.0137/0.0127/0.0150), with detected-gene recall reported and not gated (R_det@100 0.1754 / 0.2048 / 0.2080; full-atlas recall 0.0889 / 0.0977 / 0.0995); the ≥1-molecule output is reported against the original two-sided gate (F1_det > 0.261 AND P ≥ 0.38): F1_det 0.3001–0.3046 passes, **P 0.3032–0.3520 fails** — no ≥1-molecule output clears the precision floor, so it is the sensitivity arm, not the default. Against atlas-independent Kinnex long reads, **0.7647** of default PBMC sites have a poly(A)-verified 3' end within 25 bp at ≥5 UMI (0.8941 for atlas-known hexamer-pass sites; null ≈ 0.0074; `16_trusted_novel_kinnex.md` §v2, verified).
- **Exact evidence (v2 run, verified FIXED):** `score_tool.py` on `pas_PRESPEC_precision_default.bed` (PBMC IP arm n 46,524; mice n 26,255 / 26,526), `pas_tier1.bed` and `pas_tier2.bed` per arm, windows 10–200 bp, 3-seed nulls, detected-gene denominators 285,136 / 126,686 — `results/benchmark_tools/pbmc_10k_v3/peakatail_clipseeded_final_v2_ipfilt/score_pbmc_final_v2_ipfilt__PRESPEC_precision_default.tsv`, `…/gse104556/peakatail_clipseeded_final_v2/mouse{1,2}/score_testis_m{1,2}_final_v2__PRESPEC_precision_default.tsv`; reproduced to six decimals by an independent `bedtools closest`/`window` pipeline, with every movement against v1 equal to the measured #96 impact (residual 0); IP-filter delta +5.3 pp P / −1.2 pp R_det (mouse 1). Molecule unit (`pas_support.tsv`): 72.1% of PBMC tier-1 sites are single-molecule and are discarded by the default (mouse ≈ 50%); of the ≥2-molecule PBMC sites 90.2% keep ≥2 molecules under `-F 3844` (mouse 94.8 / 95.6%; v1 measurement, not re-derived in v2). **Kinnex (v2, `16` §v2, verified):** default 0.7647 / 0.5584 / 0.2807 / 0.0976 within 25 bp at ≥5 / ≥20 / ≥100 / ≥500 UMI (support in UMIs; x3p truth); atlas-known hexamer-pass complement 0.8941; atlas-novel subset 0.4853; 27.0% of atlas-novel sites at Kinnex IP decoys vs 7.6% — a genuine/IP/unsupported decomposition of the full default per tier is not computed. Motif re-anchoring: 76.7% of default sites carry a canonical hexamer in −40..−5 of the clip-seeded cleavage point; the null for that window is still to be computed. **Stage 1d (merged):** the IP-filter minus-strand fix (#96) and the performance fix (#97) landed on 2026-08-21 and the re-run gives PBMC 0.7062 (from 0.7167), gates unchanged (`19`). Verified context that stays: curated-atlas precision of the shipped arms 0.36–0.49 (~20× null at 100 bp), the ~90–105 nt cleavage-offset, IP filter 6.3% → 3.0%. **Honesty rules:** say "atlas-agreement precision (100 bp)", never bare "precision"; always report both tiers with n; never quote tier-1 precision without its recall; show R_det and full-atlas recall together; F1 only with the truth set named; recall ceilings stated; scored set = post-cell-filter set; single PBMC donor / single CellRanger BAM, the two mice one study and chemistry.

**R3. A six-tool benchmark shows a trade surface, not a ranking — and that evidence type discriminates** *(Fig 3)*
- **Key claim:** across six tools and two datasets under one scorer, precision, recall, call count and replicate reproducibility trade off; PeakATail's final default sits on the high-precision side of that surface and its ≥1-molecule arm on the sensitivity side — **verified (v2 rows `19` §1, competitor rows `15_final_gate.md` §3): the precision default is the most atlas-concordant de novo call set in the panel on both datasets (P@100 0.7062 vs SCAPTURE 0.652 on PBMC; 0.7450/0.7572 vs 0.694/0.672 on mice), at polyApipe-or-lower recall (PBMC −12%, mouse −18% / −17% vs polyApipe) and essentially tied F1 (+0.020 PBMC, +0.013 / +0.014 mouse) — the F1 lead is not a meaningful margin; the claim is precision, not overall accuracy**; the shipped coverage-only caller ranked last on PBMC (F1 0.134 vs polyApipe 0.261), and the gap was evidence type, not threshold or offset (verified).
- **Exact evidence:** v2 PeakATail rows (Fig 3A values above, from `19_final_gate_v2.md` §1–2) added to `benchmark_consolidated.tsv`; replicate concordance@100 on testis for the final default **[final run pending]**; depth stratification; long-read re-ranking (ρ 0.90 at 7/8 arms) **[quarantined; regenerate]**; fairness table (≥95% gene-proximal for all tools, verified); resource logs (Fig 3E, v2 after #97: 12.53 GB peak RSS and 27m43s uncontended on the PBMC BAM; v1 was 293.7 GB / 3:45:53 at ~1.4 cores). **Honesty rules:** report where competitors win (scUTRquant's catalog P 0.793 / 0.782 / 0.784 and F1 0.290 / 0.389 are above every de novo arm; polyApipe's R_det 0.199 / 0.250 exceeds the default's 0.1754 / 0.2048; scAPAtrap's R_det 0.300 is the highest; scAPAtrap and Sierra reproducibility above PeakATail's shipped 0.73–0.75); never claim F1 superiority (+0.015 / +0.010 over polyApipe is not a margin); state that PeakATail's reproducibility advantage is against polyApipe only; state the compute footprint plainly: v1 needed a ≥300 GB node for a 10k-cell PBMC BAM; after #97 the same BAM runs in 12.53 GB peak RSS and ~28–35 min (`19` §3).

**R4. Cell-type-specific APA switches are reported only when calibrated and replicated** *(Fig 4)*
- **Key claim:** PeakATail's shipped tests were anti-conservative (Fisher 38.6% / NB 21.3% null p < 0.05 on mis-keyed matrices; on correctly keyed matrices the shipped defaults remain anti-conservative at 20.3% reads-Fisher / 13.0% cells-Fisher with marker pre-selection / 24.7% `nb_pairwise` — `14_switch_calibration_v2.md`, verified SOUND); **only Fisher `--count-mode cells` with marker pre-selection off controls FDR (3.0% null p < 0.05, 0/20 null runs with any q < 0.05 hit; conservative)** and ships as the default, with permutation-calibrated q required whenever marker pre-selection or `nb_pairwise` is used; a switch is reported only if replicated in ≥2 samples with |ΔPDUI| ≥ 0.1 (`13_reliability_positioning.md` §2) — replication yields and between-donor specificity **[final run pending — Stage 3]**.
- **Exact evidence needed:** null p-value histograms before/after on re-keyed counts-layer matrices; permutation-q pipeline; replication-filter yields and between-donor specificity null; canonical positive controls (IGHM plasma-vs-B intronic switch; proliferation-linked shortening) recovered with correct direction at calibrated q. `--count-mode cells` (#86) is exposed and was used in `14`; tool change requested (issue 9): default `--marker-top-n 0` or a label-independent pre-filter, and the Fisher gene denominator from the full matrix regardless of mode. **Blocking dependency for replication:** the Stage-3 Laughney run.

**R5. PeakATail recovers the spermatogenesis 3'UTR shortening gradient; the pre-registered trusted-novel PAS definition misses its long-read target (negative result)** *(Fig 5)*
- **Key claim (length):** 3'UTR length decreases SPC > RS > ES in both mice under every labelling, with per-gene replicate agreement and literature genes in the expected direction; the SPG step is reported as fragile in supplement. **[final run pending; preliminary ρ −0.73..−0.86 quarantined]**
- **Result (trusted novel — NEGATIVE, `16_trusted_novel_kinnex.md` §v2, verified):** the pre-registered gate (`13_reliability_positioning.md` §3) required ≥70% of trusted-novel PBMC PAS to have a Kinnex 3' end within 25 bp; on the v2 output the definition yields 7,259 sites (15.6% of the default) and **48.5% [95% CI 47.4–49.7] are long-read-confirmed at ≥5 UMI — gate NOT MET** under every truth choice (GEM-X 0.5154, pooled 0.5816), against a shuffled null ≈ 0.0074 (enrichment ~65–180×) and an atlas-known calibration of 0.8941; the v1 value was 52.1%, so the Stage-1d fixes moved the result down, not up, and the negative result is not an artefact of the IP bugs. Residual error: 69.4% intronic, 27.0% at Kinnex internal-priming decoys (vs 7.6%). Reported as a negative result with the funnel figure; post-hoc stricter strata (3'UTR 0.769, ≥5 molecules 0.692–0.728) are exploratory only; **no "trusted novel" claim**.
- **Exact evidence:** spermatogenesis — regenerated panels on re-keyed matrices with both "by peak count" and "by UMI signal" framings and the `wul`/`wdi` discrepancy resolved **[final run pending]**; trusted novel — funnel counts, Kinnex support fraction with null and CI, robustness table across truths/windows, composition (feature class, IP-decoy rate), exploratory strata flagged as post hoc (`results/reliability/trusted_novel_final_v2_pbmc/`, verified; v1 in `results/reliability/trusted_novel_final_pbmc/`).

**R6. Application: replicated per-cell-type APA switches across 17 lung-cancer samples** *(Fig 6; pending Amir's rerun)*
- **Key claim:** per cell type, a set of APA switches across Normal → Stage I → Metastasis that replicate across samples under the calibrated test and replication filter; a TAM/myeloid 3'UTR program as the worked example with orthogonal support for ≥1 gene.
- **Exact evidence needed:** #67 SWITCH_CELLTYPE rerun on re-keyed, counts-layer matrices with the fixed `main.nf` (unified merged bed), `--count-mode cells`, replication statistics, effect-size correlation across samples. **Fallback:** fold into Fig 4 if the rerun slips; the methods + benchmark paper stands without it.

### Discussion
- What "trusted" means operationally and why precision-first with explicit tiers is the right default for a field where most published call sets have never been scored against a null.
- The benchmark lessons: evidence type over peak-calling sophistication; per-dataset reporting; atlas-only validation flatters internal-priming artefacts; F1 is truth-set-size dependent.
- The calibration lessons: pseudoreplication, double-dipping, dispersion floors, and the value of permutation nulls and replication filters (only one of six test configurations controlled FDR on correctly keyed data; `14`).
- The de novo lesson: an atlas-novel call set can be 65–180× enriched over a positional null and still miss an absolute long-read target (49% vs 70%); intronic sites and filter-missed internal priming dominate the residual, and any stricter definition chosen after seeing the data is exploratory until validated on held-out data (`16` §v2).
- Interpretation of spermatogenesis and Laughney findings against the APA literature (scoped to what replicated).
- **Limitations (state plainly):** the clip-evidence recall ceiling (~29% of detected-gene atlas sites carry any clip read in the PBMC BAM; chemistry/aligner dependent — poly(A)-trimmed pipelines lose the evidence entirely) — the default's R_det is 0.1754 (PBMC) / 0.2048 / 0.2080 (mice) and full-atlas recall 0.0889 / 0.0977 / 0.0995, below polyApipe's; **compute is no longer a limitation — the 294 GB paragraph moves to a "resolved in Stage 1d" note: the v1 caller needed 293.7 GB peak RSS and 3:45:53 on the 10k-cell PBMC BAM at ~1.4 cores despite `--threads 16` (a ≥300 GB node); after #97 the same BAM runs in 12.53 GB peak RSS and ~28–35 min (27m43s uncontended; 34:37 with all four arms concurrent), `19` §3**; the precision-first default and its gate were pre-registered (01:19) before the final run (02:59) but after a stale run failed the original gate and after a post-hoc sweep informed the ≥2-molecule threshold (disclosed, S9); "precision" is atlas-agreement at 100 bp, so atlas-novel true sites count against us (the Kinnex check: 0.7647 of default PBMC sites within 25 bp of a long-read 3' end at ≥5 UMI, but only 0.4853 of the atlas-novel subset — the pre-registered trusted-novel gate was missed, `16` §v2); the v1 run's IP filter applied the forward-coordinate window on both strands (Stage 1d; found by the verifier, fixed in #96, merged and re-run: PBMC P@100 0.7167 → 0.7062, gates unchanged, `19`) and the rule is still looser than Kinnex's, so residual internal priming remains in the default (27.0% of the atlas-novel subset sits at a Kinnex IP decoy, `16` §v2); the switch-test calibration rests on one mouse and one tissue until the Laughney replication lands; the gate was met with a large margin but on three call sets from two datasets (one PBMC donor, one CellRanger BAM; the two mice are one study and chemistry); the PAS-profile clustering is an expression proxy, not isoform-choice discovery; Laughney samples are donor-correlated; no Read1 cleavage mode; the quarantined first-version Kinnex figure counted long-read support in records, not molecules (`16` counts UMIs and supersedes it).
- Future: Read1/TVN-priming cleavage mode, sequence-model scoring as a third evidence channel, spatial.

### Methods
1. Datasets & preprocessing (STARsolo/CellRanger as documented; whitelists; GEO/atlas accessions; Kinnex FLNC processing).
2. Read ingestion: UMI dedup, SAM-flag filtering (−F 3844), clip-site detection (`min_clip`, `min_purity`), wrong-end specificity control.
3. PAS calling: clip-seeded clustering (25 bp single-linkage, read-weighted mode, `--polya-min-reads` in distinct molecules), coverage-only tier-2, multi-PAS splitting; the legacy strategies as registry baselines.
4. Internal-priming filter: genomic A-tract definitions (−10..+30, ≥6 A or ≥70% A), `annotate` vs `filter` modes; the strand-symmetric window fix (#96, merged 2026-08-21; v1 runs applied the forward-coordinate window on both strands) and `ema reannotate --genome-fasta` to recompute flags on pre-fix (v1) runs; the Kinnex-style rule (+1..+18, ≥12/18 A) as the still-open `--ip-rule kinnex` option (issue #95 addendum).
5. Annotation: tiered gene-end/3'UTR assignment (`max_gene_distance`, `utr_multiplier`, TIER_3 handling); note the TIER label reflects gene assignment, not 3'-end proximity.
6. Output tiers and the pre-registered default (`--peak-strategy clip_seeded --ip-filter --ip-filter-mode filter`, tier-1 ∩ IP-pass ∩ ≥2 distinct clip molecules; pre-registered `0e27b1a`, 2026-08-21 01:19, before the 02:59 run); `low_confidence` file; note that in `filter` mode `annotatedpas.bed` carries no per-site IP flag and that `pasbed.bed` drops zero-count gene-assigned PAS after the cell filter (0.45% PBMC, 3.7% mouse 1) — the scored set is the post-cell-filter set.
7. Differential APA: within-gene Fisher with `count_mode="cells"` and marker pre-selection off (`--marker-top-n 0`) as the reliable default (`14`); NB with shrinkage; permutation-calibrated q mandatory with marker pre-selection or `nb_pairwise`; Fisher gene denominator from the full matrix; replication filter; effect-size floor.
8. Length metrics: PDUI-classic (proximal/distal rule, strand-correct), proportion, entropy; structural direction; `switch trend`.
9. Trusted-novel PAS definition (pre-registered, `13` §3) and long-read validation protocol (Kinnex x3p / GEM-X truth, 25 bp strand-matched, UMI support tiers, gene-body-shuffled null, Wilson CI); outcome reported in `16` as a negative result.
10. Validation protocol: curated PolyASite 2.0 point sites (569,005; GRCh38.96), protein-coding TES, detected-gene denominators (285,136 PBMC / 126,686 testis), windows, gene-body-shuffled nulls (3 seeds), atlas-agreement precision / R_det / full-atlas recall / F1_det definitions, motif and IP analyses; atlas-free runs for scoring (no snap circularity); `LC_ALL=C` for every sort/bedtools step.
11. Benchmarking protocol: tool versions, parameters, environment fixes, hardware, resource measurement, `score_tool.py`.
12. PAS-profile clustering (TF-IDF/LSI/Leiden) as an optional utility, with the gene-sum ablation.
13. Reproducibility: frozen commit per run, `run_config.json`, content-hashed `run_manifest.json`, drop ledgers + survivor invariant, `reannotate` branching; code/data availability (MIT, GitHub + Zenodo DOI, all manifests as supplement).

---

## (f) Positioning vs existing single-cell APA tools (honest, reliability-centred)

**Defensible differentiators (each tied to evidence in this repo):**
1. **Evidence-tiered output with per-site molecule support.** Clip-supported vs coverage-only tiers, UMI-deduplicated clip counts in the BED score column, IP flag per site — users can see *why* a site was called. No compared tool exposes this.
2. **Pre-registered precision-first default and published gates.** The default output and its gates were fixed in writing before the final run (`0e27b1a` 01:19 vs arms 02:59, 2026-08-21) and the gate passed on all three call sets, and passed again unchanged after the Stage-1d re-run (v2 P@100 0.7062 / 0.7450 / 0.7572 vs ≥ 0.50; v1 0.717 / 0.741 / 0.755); the stale run's failure and the post-hoc sweep that informed the threshold are disclosed (S9).
3. **Calibrated, replicated switch testing.** Null-calibration as a shipped check, permutation-q fallback, replication filter across samples, effect-size floor — a documented answer to pseudoreplication and double-dipping.
4. **Atlas-independent long-read test of de novo PAS** against a positional null — reported honestly as a negative result (49% vs the 70% target on the v2 output, `16` §v2); de novo sites ship with molecule support, hexamer tier, feature class and IP status, not a "trusted" label.
5. **Benchmark methodology:** one scorer, curated point-site reference, per-dataset denominators, gene-body-shuffled nulls, replicate reproducibility, long-read re-ranking, the cleavage-offset finding, and the evidence-type result.
6. **Engineering/reproducibility layer:** aligner-agnostic input (STARsolo, CellRanger, Alevin-fry — three CellRanger-input bugs found and fixed, with a fixture in CI), `reannotate` branching, append-only drop ledgers with survivor invariant, content-hashed manifests, frozen commit per run.
7. **Optional utility:** PAS-profile clustering for label-free exploration when no GEX labels exist — explicitly an expression proxy (S4).

**What we must NOT claim:**
- **Accuracy leadership** beyond what the final-run gates show; never a single cross-dataset accuracy number; never F1 without the truth set named.
- **"Precision" without "atlas-agreement"** — every precision number is atlas-agreement precision at 100 bp against curated PolyASite 2.0 point sites; atlas-novel true sites count as false positives; the Kinnex long-read check (`16`) is the atlas-independent complement and is quoted alongside.
- **F1 superiority** — the default's F1_det lead over polyApipe (+0.020 PBMC / +0.013–0.014 mouse) is not a meaningful margin and scUTRquant's catalog F1 is higher; the claim is precision, not overall accuracy.
- **The non-IP PBMC ≥2-molecule file as the default** — `pas_tier1_ge2mol_noIP_POSTHOC.bed` (n 62,110, P 0.591) has no IP filter and is not the pre-registered default; the default is the IP-arm `pas_PRESPEC_precision_default.bed` (v2 n 46,524, P 0.7062).
- **"Trusted novel PAS" as a positive claim** — the pre-registered definition reached 49% (not 70%) Kinnex concordance at 25 bp on the v2 output, 52% on v1 (`16` §v2); stricter variants were chosen after seeing the data and must be pre-registered and validated on held-out data before any de novo site is called "trusted". The funnel is reported as a negative result.
- **Recall parity** — the default's R_det is below polyApipe's (−12% PBMC, −18% / −17% mouse) and must be shown with full-atlas recall.
- **Reproducibility leadership** — the shipped caller's advantage is against polyApipe only; scAPAtrap and Sierra are above it.
- **Clustering novelty or "cell identity from PAS usage alone"** — the ablation shows gene totals carry the signal.
- **FDR-controlled discovery** from the shipped Fisher/NB q-values; only calibrated or permutation-calibrated q-values after the fix.
- **The retired dump-based precision figure**, anything against the 18.4M dump, or atlas snap-rate as precision (circularity).
- **Stale Stage-2 numbers** as results; **quarantined spermatogenesis/Kinnex numbers** until regenerated per `11_verifier_corrections.md`.
- **Monotone four-stage spermatogenesis shortening** — the SPG step is fragile; claim three stages.
- Read1 cleavage-site precision, sequence-model filtering, spatial support — roadmap items.

**Positioning sentence for the Introduction:** "Rather than asking how many poly(A) sites a single-cell caller can report, PeakATail asks how many can be trusted: it seeds sites from poly(A)-tail read evidence, labels every call with its evidence tier and molecule support, tests cell-type switches only where they are calibrated and replicated, and reports its novel sites with their long-read validation outcome, including the negative one — within a single aligner-agnostic, provenance-tracked framework whose acceptance gates were registered before the results existed."

---

## Pre-submission checklist (condensed)
- [x] Stage 1c merged (UMI-dedup clip evidence, −F 3844, BED score = molecules); final Stage-2 run launched from a frozen commit with the pre-registered default — done: frozen worktree `tools/pa-polya-run-4efeb125`, run 2026-08-21 02:59–06:46, verified SOUND (`15_final_gate.md`); superseded by the v2 re-run from `tools/pa-polya-run-9dfdefb3` (v2 verified, `19`).
- [x] Fill every Stage-2 [final run pending] bracket from the final run only; attach gate table (T3) with pass/fail — done 2026-08-21 from `15_final_gate.md` (Fig 2A/B/E/F, Fig 3A/E, R2, R3, S7, S9, abstract, limitations), `16_trusted_novel_kinnex.md` (Fig 2C, Fig 2D in part, Fig 5D–F, R5 trusted-novel as a negative result, T5) and `14_switch_calibration_v2.md` (Fig 4A/B, R4, S6, Methods 7); all benchmark and Kinnex numbers moved to the Stage-1d re-run on 2026-08-21 (v2 verified, `19`; `16` §v2).
- [ ] Fill the Stage-3 brackets (switch replication, spermatogenesis on the final caller, Laughney per-cell-type, Fig 3C/D, Fig 4C/D, Fig 5A–C) — runs in progress; Fig 2D still needs a null for the re-anchored motif window.
- [x] Stage 1d re-run: #96 (IP-filter strand fix) and #97 (performance) merged 2026-08-21; the four arms re-run from the frozen worktree `tools/pa-polya-run-9dfdefb3` (code `9dfdefb`) and re-scored — PBMC P@100 0.7167 → 0.7062, mice 0.7450 / 0.7572, gates unchanged, every prediction hit to ≤ 0.001 (v2 verified, `19`); Fig 2 / T3 / abstract numbers refreshed here and the trusted-novel funnel re-derived on the re-run (7,259 sites, 0.4853 — negative result stands, `16` §v2). Still open: `--ip-rule kinnex` (issue #95 addendum); figures regenerating on v2.
- [ ] Regenerate spermatogenesis and Kinnex figures on re-keyed matrices with the `11_verifier_corrections.md` corrections as spec; resolve `wul`/`wdi`.
- [ ] FDR recalibration on counts-layer, re-keyed matrices (`--count-mode cells`, NB shrinkage); decide permutation-q default before freezing Methods — calibration decided per `14` (Fisher cells, marker off, nominal BH; permutation-q required otherwise); remaining: confirm on the final caller's matrices and on Laughney.
- [ ] Amir's #67 SWITCH_CELLTYPE rerun (fixed `main.nf`, counts layer, replication filter) — decides whether Fig 6 ships.
- [ ] Re-run the novelty usage-space ablation on re-keyed matrices for S4 (does not change the claim).
- [ ] Atlas-free scoring runs for Fig 2 (confirm from the four `run_config.json`s and state in Methods 10); Kinnex support is counted in UMIs in `16` (done).
- [ ] Real-cohort provenance reconcile (`reconcile_summary.json` all_ok) on the final Laughney run.
- [ ] Literature sweep of 2025–26 single-cell PAS callers using clip evidence or long-read truth (positioning, not novelty, risk).
- [ ] bioRxiv preprint + Zenodo DOI + Docker image before journal submission.
